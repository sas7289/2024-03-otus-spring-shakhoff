spring:
  mongodb:
    embedded:
      version: 4.0.2
  shell:
    interactive:
      enabled: false
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.0.2
mongock:
  enabled: true
  mongo-db:
    write-concern:
      journal: false
    read-concern: local
  change-logs-scan-package:
    - ru.otus.hw.changelog